# Pomodoro


<!--more-->

 superpower

1. 将核心逻辑发送并让他补充. 通过askQuestionuser

提示词:
```
我想要做一个番茄钟加任务管理的 Windows 应用，它的核心逻辑如下：核心逻辑：
  1.番茄钟必须要绑定一个任务才能开始 ， 允许用户一键创建一个临时任务.( 专注页顶部或显眼位置放一个“⚡快速开始”按钮。点击后只需输入任务名，默认分配 1 个预期番茄钟，无子步骤，直接开跑。).

  2.用户可以设定当前任务需要多少个番茄才能完成。程序会在最后一个番茄完成后，弹窗向用户询问：是否任务完成来判断是否需要需要加番茄或者任务状态改为结束(文案： “恭喜！本任务预期的 [N]
  个番茄钟已完成。任务做完了吗？”
  选项 A： “完美搞定” -> 撒花动效，任务状态自动流转为 Done，返回任务列表。
  选项 B： “还差一点，追加番茄” -> 出现快捷按钮（+1个, +2个），任务继续保持 Doing 状态。)

  3.允许用户提前结束任务,弹窗提问用户是否番茄生效(选项 A： “记录实际时间 (15分钟)” -> 番茄钟算作完成，但记入统计时按15分钟算（适合精确追踪者）。
  选项 B： “作为完整番茄钟记录” -> 算作1个完整番茄（适合宽容度高的用户）。
  选项 C： “作废此番茄” -> 适合刚点开始 1 分钟就发现不需要做的误操作。)

  4.层级：项目(求职学习)->任务(面试准备)-> 目标(Java基础,jvm) -> 子步骤(1.打开面试刷题网站 2.先给出自己的答案3.看参考答案修正.4再给出自己的答案)  番茄钟挂在任务层级，
  子步骤作为打钩清单显示任务详情下面.

  5.临时任务完成后，在“任务管理页”里应该有一个专门的**“收集箱 (Inbox)
   你可以使用AskUserQuestion工具向我询问任何觉得模糊的,阻碍你开发的点. 澄清他们直到你认为所有都是确定的.
```


2. 澄清问题

这里确定技术栈

信息架构与核心数据模型


澄清异常状态,澄清
给出spec 文档










写plan文件


## 收获

git 管理自己来. 

---

> 作者: cheems  
> URL: http://localhost:1313/ai/%E5%AE%9E%E6%93%8D/f8b3fa0f/  

